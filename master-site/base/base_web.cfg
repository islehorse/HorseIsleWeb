# Database credentials for the web server specifically;

DB_NAME=web
DB_USERNAME=horseisle
DB_PASSWORD=test123
DB_HOST=localhost

EMAIL_ACTIVATION=false
FROM_EMAIL_ADDR=support@horseisle.com

PP_URI=[GAMESITE]/web/ppemu.php
# location of paypal emulator on game-servers
# [GAMESITE] is replaced with the URL for the game-site, as specified in servers.php
# original is https://www.paypal.com/cgi-bin/webscr which obviously wont do
# Dont set it to that though, as the paypalgateway.php is not (and probably never will be.) implemented.

HMAC_SECRET=!!NOTSET!!